name: tessera_finance
version: '1.0.0'
config-version: 2

profile: tessera_finance

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  tessera:
    project_name: tessera_finance
    owner_team: finance-analytics
